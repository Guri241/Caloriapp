export default function HomePage() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: 24 }}>
      <h1>Caloriapp API</h1>
      <p>This service only exposes REST endpoints under /api/*.</p>
    </main>
  );
}
